image: julia:1.7

stages:
  - build
  - test

build:
  stage: build
  tags:
    - ci.inria.fr
  script:
    - julia --project=@. -e "import Pkg; Pkg.build()"

test:
  stage: test
  tags:
    - ci.inria.fr
  script:
    - julia --project=@. test/runtests.jl